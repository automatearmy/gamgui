## API Configuration

# Google Cloud Project ID
PROJECT_ID=your-gcp-project-id

# GCP region
REGION=us-central1

# Application environment (development, staging, production)
ENVIRONMENT=development

# API server port
PORT=8000

## Registry Configuration (Artifact Registry for container images)
# Project ID for artifact registry
REGISTRY_PROJECT_ID=your-registry-project-id

# Region for artifact registry
REGISTRY_REGION=us-central1

# Repository name in artifact registry
REGISTRY_REPOSITORY_NAME=gamgui

# Container image names
BACKEND_IMAGE_NAME=gamgui-backend
FRONTEND_IMAGE_NAME=gamgui-frontend
SESSION_IMAGE_NAME=gamgui-session

## OAuth Configuration
# Service account email for backend
BACKEND_SERVICE_ACCOUNT_EMAIL=gamgui-backend@your-project-id.iam.gserviceaccount.com

# OAuth client ID for backend authentication
BACKEND_OAUTH_CLIENT_ID=your-backend-oauth-client-id

# OAuth client secret for backend authentication
BACKEND_OAUTH_CLIENT_SECRET=your-backend-oauth-client-secret

# OAuth client ID for frontend authentication
FRONTEND_OAUTH_CLIENT_ID=your-frontend-oauth-client-id

# OAuth client secret for frontend authentication
FRONTEND_OAUTH_CLIENT_SECRET=your-frontend-oauth-client-secret

## Authentication
# JWT secret for token generation and validation (defaults to combination of OAuth secrets)
# JWT_SECRET=your-custom-jwt-secret

## Kubernetes Configuration
# GKE cluster name for session pods
CLUSTER_NAME=gamgui-sessions

## Logging
# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
